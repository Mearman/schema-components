/**
 * schema-components — React components that render UI from Zod schemas,
 * JSON Schema, and OpenAPI documents.
 *
 * This is the root entry point. Sub-path exports are in their own directories:
 * - schema-components/react  — <SchemaComponent>, <SchemaField>, hooks
 * - schema-components/core   — Zod schema walker, component resolver
 * - schema-components/openapi — OpenAPI document parsing
 * - schema-components/themes/* — theme adapters (shadcn, MUI, etc.)
 */
